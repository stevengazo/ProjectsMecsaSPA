@page "/search-customer"
@inject IDbContextFactory<ProjectsDBContext> DbContextFactory

<h3>Buscar Clientes</h3>

<EditForm Model="searchModel" OnValidSubmit="ApplyFilters">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="mb-3">
        <label for="name">Nombre</label>
        <InputText id="name" @bind-Value="searchModel.Name" class="form-control" />
    </div>

    <div class="mb-3">
        <label for="type">Tipo</label>
        <InputSelect id="type" @bind-Value="searchModel.Type" class="form-select">
            <option value="">Seleccione un tipo</option>
            <option value="Publico">Público</option>
            <option value="Privado">Privado</option>
        </InputSelect>
    </div>

    <button type="submit" class="btn btn-primary">Buscar</button>
</EditForm>

@if (customers != null)
{
    <table class="table table-striped mt-3 table-hover">
        <thead>
            <tr>
                <th>
                    <button @onclick="() => SortByColumn(nameof(Customer.Name))" >Nombre</button>
                </th>
                <th>
                    <button @onclick="() => SortByColumn(nameof(Customer.Type))" >Tipo</button>
                </th>
                <th>
                    <button @onclick="() => SortByColumn(nameof(Customer.DNI))" >DNI</button>
                </th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var customer in customers)
            {
                <tr>
                    <td>@customer.Name</td>
                    <td>@customer.Type</td>
                    <td>@customer.DNI</td>
                    <td>
                        <a href="/customers/@customer.CustomerId" class="btn btn-sm btn-outline-info">
                            Ver
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    private SearchModel searchModel = new();
    private List<Customer> customers = new();

    private string currentSortColumn;
    private bool isAscending = true;

    private async Task ApplyFilters()
    {
        using (var db = DbContextFactory.CreateDbContext())
        {
            var query = db.Customers.AsQueryable();

            if (!string.IsNullOrEmpty(searchModel.Name))
            {
                query = query.Where(c => c.Name.Contains(searchModel.Name));
            }

            if (!string.IsNullOrEmpty(searchModel.Type))
            {
                query = query.Where(c => c.Type == searchModel.Type);
            }

            // Aplicar el ordenamiento
            if (!string.IsNullOrEmpty(currentSortColumn))
            {
                query = isAscending ? query.OrderBy(c => EF.Property<object>(c, currentSortColumn))
                                    : query.OrderByDescending(c => EF.Property<object>(c, currentSortColumn));
            }

            customers = await query.ToListAsync();
        }
    }

    private void SortByColumn(string columnName)
    {
        if (currentSortColumn == columnName)
        {
            isAscending = !isAscending;
        }
        else
        {
            currentSortColumn = columnName;
            isAscending = true;
        }

        ApplyFilters();
    }

    public class SearchModel
    {
        public string Name { get; set; }
        public string Type { get; set; }
    }
}
